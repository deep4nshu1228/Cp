#include <bits/stdc++.h>
#define all(n) (n).begin(), (n).end()
using namespace std;
typedef long long int ll;
void solve(){
    string a,b;
    cin >> a >> b;

    if(a == b) cout << a << "\n";
    else cout << "1\n";
}
int main()
{
    solve();
    return 0;
}