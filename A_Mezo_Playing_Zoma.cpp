#include <bits/stdc++.h>
#define all(n) (n).begin(), (n).end()
using namespace std;
typedef long long int ll;
int main()
{
    int n;
    cin >> n;

    string s;
    cin >> s;

    // int left = count(all(s),'L');
    // int right = count(all(s),'R');

    cout << n + 1 << "\n";
    return 0;
}